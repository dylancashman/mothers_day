<%= form_for(@celebration) do |f| %>
  <% if @celebration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@celebration.errors.count, "error") %> prohibited this celebration from being saved:</h2>

      <ul>
      <% @celebration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email_address %><br />
    <%= f.text_field :email_address %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
